package com.testgioco.core.interfaces.base;

public interface Visible {
    /**
     * Returns the visibility state of the element.
     * */
    boolean isVisible();

    /**
     * Sets the visibility state of the element.
     * */
    void setVisible(boolean isVisible);
}
