package com.testgioco.core.tile;

/**
 * Contains every tile's name.
 * Every name should correspond to "assets/tiles/<tile_name>.png"
 * */
public enum TilesName {
    grass_01,
    wall_01,
    water_01,
    not_loaded
}
